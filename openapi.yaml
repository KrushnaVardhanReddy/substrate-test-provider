openapi: "3.0.0"
info:
  title: Provider API
  version: "1.0.0"
paths:
  /users:
    get:
      summary: List users
      responses:
        "200":
          description: OK
  /users/details/safe-endpoint:
    get:
      summary: A new completely safe endpoint
      responses:
        "200":
          description: Safe Addition
