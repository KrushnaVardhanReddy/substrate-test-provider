asyncapi: '2.6.0'
info:
  title: User API
  version: 1.0.0
channels:
  users/signup:
    x-deprecated: true
    publish:
      message:
        payload:
          type: object
          properties:
            username:
              type: string
